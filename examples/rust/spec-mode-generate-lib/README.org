* README

Run the generator with the spec in this folder

The spec file is [[file:spec.lisprpc][spec.lisprpc]].

After build the ~lisp-rpc-rust-generator~

#+begin_src shell
  lisp-rpc-rust-generator \
      --input-file spec.lisprpc \
      --output-path ~/Desktop/
#+end_src
