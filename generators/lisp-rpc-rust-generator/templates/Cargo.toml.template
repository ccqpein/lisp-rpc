[package]
name = "{{ package_name }}"
version = "0.1.0"
edition = "2024"

[dependencies]
lisp-rpc-rust-serializer = { git = "https://github.com/ccQpein/lisp-rpc" }
serde = { version = "1", features = ["derive"] }